{
  "version": 2,
  "builds": [
    {
      "src": "yarn build",
      "use": "@vercel/static",
      "config": {
        "includeFiles": {
          ".prisma/**": true
        }
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "yarn build" }]
}
